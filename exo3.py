# verificador do ano bissxto

using System .Data;
Using System .Drawing;
using System .Línq;
using system .windows . forms;

namespace verificador_Ano_Bissxto
{
      input partial class formal : forma
      {
        inializecomponent():
      }
      void verificarAno()
      int ano . convert.toint32 (numericUpDown1.value);
      if (((ano % 400)==0 ||(ano % 4 == 0n88 ano % 100 | = 0) )
          {
              inabeli.Text "Ano Bissexto!";
         else
              iabeli.Text "Ano Bissexto!";
